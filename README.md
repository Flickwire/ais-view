# AIS Realtime Viewer

Displays realtime AIS position updates from aisstream.io (When it is up, API seems to be overwhelmed currently)

Currently this will consume all of your browser's available memory if left to run to long, cleaning this up is a future to-do. (useAisData.ts)
